# GOAI 世界人工智能开源大赛 · 赛道三 AI for Research
# 算法赛初赛方案说明文档

> **【状态说明 · 定稿前删除本段】**
>
> **正文六节已全部成稿。** 第二节的科学判断由参赛者本人口述后整理成文，
> 立场未作改动；第三节描述的是已建成的技术方案与已有实测证据。
>
> 仅余三处需你提供的**事实性信息**（非撰写），已在原地用 `【需确认】` 标出：
> 开源仓库地址、过往成果、项目名称是否沿用本文拟定。
>
> 全文数字的单一来源：`results/AUTHORITATIVE.md`（由脚本现场重算，附口径与代码指纹）。
> 提交前请跑 `python scripts/data/doc_number_check.py` 核对，现状为 25/25 全部对上。
> 图见 `results/figures/`，正式稿建议用同名 `.pdf` 矢量版。

---

# 一、项目概述

## 1.1 项目名称

**不依赖测试对照的虚拟酵母扰动蛋白质组预测：以可提交性与可验证性为先的方案**

> 【需确认】此名称由本方案的两条主张倒推而来——「模型在推断时不读取测试对照」
> 与「每条结论都必须能被独立复核」。若你偏好更简短的写法，备选：
> 「虚拟酵母扰动蛋白质组预测：一个 C-free、可验证的低秩条件模型」。

## 1.2 参赛方向

赛道三 AI for Research · **算法赛题 · 方向一：虚拟细胞（虚拟酵母扰动响应预测）**

主榜任务：使用组委会提供的 train_val 数据，学习跨菌株、化学扰动、培养基、温度、
时间点与测量上下文的蛋白质组响应规律，预测独立测试集样本的完整 log2 蛋白质组向量。
考察三类泛化：未见化合物（S1）、未见菌株（S2）、双重未知（S3）。

## 1.3 方案概述

本方案面向方向一的主榜任务，预测每个测试样本的完整 5,243 维 log2 蛋白丰度向量。

**技术方案**的形态由一个工程约束反推而来：评分器用实测对照计算扰动量，
但模型在推断时能否读取测试集对照，取决于测试集蛋白质组文件的可用性；
在该文件的状态获得组委会澄清前，我们按保守假设处理（见 5.3），
因此模型必须**不依赖测试对照**即可产出预测。据此确定的形态是

```
ŷ = b(实验条件与测量上下文) + γ · Δ(化合物, 上下文)
```

其中 `b` 是条件性绝对状态骨架，输出侧采用低秩表示；`Δ` 是强收缩的扰动响应残差；
`γ` 由验证决定。缺失值全程保留掩码、不填补，损失只作用于可观测位置。

**预期方法路线**分三步：先建立可信的测量与验证基础（复刻官方六项指标评分器、
建立对照匹配管线，两者均附单元测试），再用基线阶梯与严格的整化合物留出确定
各模块的边际价值，最后只保留那些在分组 bootstrap 不确定性之外仍成立的组件。
所有参照统计仅由训练折计算并冻结；模型选择一律使用官方划分与整实体留出，
不使用随机行划分。

**当前进度**：验证基础设施与诊断已完成，C-free 可部署骨架已跑通并优于全部
需读对照的诊断基线；化学结构表示经严格留出检验后未通过随机标签对照，已排除出主线。
详见第四节。

---

# 二、科学问题理解

## 2.1 科学问题与研究对象

### 任务的形式

本方向要求：给定酿酒酵母在特定菌株、化合物、培养基、温度、时间与测量上下文下的
扰动蛋白质组训练数据，预测独立测试样本的完整 5,243 维 log2 蛋白丰度向量，
并考察三类泛化——未见化合物（S1）、未见菌株（S2）、二者叠加（S3）。

这不是一个纯粹的回归拟合问题。它要求模型把"细胞在某个条件下本来是什么状态"
与"某个化合物会把这个状态推向哪里"这两件事分开表示，
否则无法在实体从未出现过的划分上外推。

### 这类任务在领域中的位置

近年提出的 AI 虚拟细胞构想（Bunne et al., *Cell* 2024）明确列出两项能力：
预测**从未在实验室测试过的扰动**的响应，以及在**具体细胞上下文**中预测该响应。
本题的三类泛化划分与这两项能力直接对应。

但我们不认为本题等同于该构想中的任何单一组件。该构想中的"操纵器"是表示到表示的变换，
而本题要直接输出实测蛋白丰度——中间还必须包含蛋白质组解码与测量过程建模。
更准确的定位是：**单物种、单模态、bulk 的条件性扰动蛋白质组预测基准**，
其能力目标与虚拟细胞构想对齐，但不宣称构成一个完整的虚拟细胞。

### 这条学术线索的来处

把本题看作一道新出现的机器学习竞赛题会低估它。它所属的范式——
**扰动 → 高维分子签名 → 机制推断**——在酵母中已有二十余年历史。

2000 年，Hughes 等人在 *Cell* 用 **300 种突变与化学处理**建立酵母表达谱图谱，
仅凭模式匹配就指派了多个未知基因的功能、并发现了常用药物 dyclonine 的新靶点。
此后，酵母凭借其独有的全基因组敲除库（21,000 余个突变株）发展出化学基因组学：
一个化合物跨数千个缺失株形成的敏感性指纹，被用来反推靶点与通路。
在人类细胞侧，同一范式演化为 Connectivity Map 与 L1000 平台
（直接测 978 个 landmark 基因、推断其余，累计 130 万条谱）。
读出模态最接近本题的一支，是近年建立的扰动蛋白质组签名库（90 种药 × 6 个细胞系）。

值得指出的是：**当年 Hughes 等人用表达谱图谱破解机制的那个化合物 dyclonine，
正在本赛训练集中，共 80 个样本。** 二十六年后，同一个化合物、同一个物种，
读出从转录组换成了蛋白质组。本题是这条线索的最新一站，不是一次孤立的算法比赛。

### 我们认为真正的困难在哪

不在数据量，而在**可迁移实体的数量**。

数据表面有 8,958 行，但独立化学实体只有 **43 个**，独立菌株只有 **5 株**；
官方训练划分中的非对照化合物更少，仅 **37 个**。同一化合物在不同批次的多行样本，
只提高对**该化合物**效应的估计精度，不会变成新的化学监督样本。

**我们的判断是：43 个化合物太少，不足以学出可外推的化学规律。**
这不是对数据质量的抱怨，而是一个有后果的判断——它意味着任何依赖
"从分子结构学到化学空间到响应的映射"的路线，在本数据规模下都缺乏统计支撑；
模型容量必须按 43 而非 8,958 来估算；化合物侧的任何组件都必须在
**整化合物留出**下验证，并与随机标签对照比较，否则无法排除偶然。
第四节报告的实测结果与这一判断一致。

未见菌株的问题性质不同但同样严峻：它占测试集的 **50.1%**，
且菌株为匿名代号、不提供任何基因型信息。在零外部信息的前提下，
菌株特异的药物响应在统计上不可识别——这是信息上限，不是模型能力问题。
我们认为诚实的做法是明确声明这一边界，而不是用复杂模型制造可预测的假象。

### 一个我们尚未确定的判断

本题把读出从"生长表型"升级到"完整蛋白质组"，名义维度从个位数跃升到 5,243。
但**高维读出是否等于高机制分辨率，我们认为并不确定。**

一方面，化学-遗传指纹虽然每个菌株只测一个适应度值，
但它跨数千个基因型构成的仍是高维指纹，且直接反映功能依赖与遗传脆弱性，
可能更贴近药物的直接靶点。另一方面，蛋白质组测量的是分子状态，
其中可能包含大量下游的共同胁迫响应。

我们自己的数据支持这一疑虑：化合物平均响应矩阵**解释 50% 方差只需 3 个方向，
80% 需 13 个，95% 需 27 个**——名义 5,243 维，有效维度是两位数。
同时，同一化合物在同样生物条件下跨批次重测，两条响应向量的一致性仅 **0.115**。

因此我们倾向于认为：**蛋白质组读出看得更细，但噪声也更大，
在稳健性上未必优于传统的生长表型读出。**

⚠ 需要说明的是，这是一个**待检验的判断，不是已证实的结论**。
我们只有本数据集的重测一致性，没有与历史化学基因组学数据在同一可靠性定义下
做过配对比较。若要证实，需要在相同的复制结构与相同的一致性指标下直接比对两类读出。
我们把它作为一个明确的、可证伪的假设提出，而不是作为既定事实使用。

这个判断直接影响建模策略：如果读出本身噪声占优，
那么正确的做法不是堆高模型容量去拟合单样本响应，
而是把监督信号建立在**聚合后的响应**上，并对单样本预测施加强收缩。

## 2.2 科学意义

我们认同官方手册的定位：本方向的价值在于**为菌株定向改造与优化提供数据支撑**，
是"AI 预测 → 湿实验验证"闭环中的一环。

以我们所在的天然产物生物合成与酶工程方向为例，这一价值的具体形态是：
当目标是让某个异源途径或某个酶在酵母中获得更高的表达与活性时，
可供调节的变量（培养基、温度、诱导时间、以及各类小分子处理）组合空间很大，
而每一个组合的湿实验验证成本都不低。一个能够预测扰动后完整蛋白质组的模型，
其直接用途是**在实验之前对这些条件组合排序**——
把有限的实验通量投向最可能改变目标蛋白丰度的那些条件。

这也解释了为什么本题的评分设计中，绝对保真度与扰动响应两类指标必须同时考察：
对改造决策而言，"这株酵母在这个条件下整体是什么状态"与
"加了这个处理之后哪些蛋白会动"同样重要，缺一不可。

需要克制的是对这一价值的过度承诺。基于第四节的实测结果，
我们认为在当前数据规模下，模型能够可靠支撑的是**条件层面的排序与筛选**，
而不是对某个未见化合物在某个未见菌株中的机制作出可直接采信的预测。
把可预测的边界说清楚，本身就是让这类模型能真正进入实验决策的前提——
一个不声明适用边界的预测，实验人员无法据以分配资源。

# 三、技术方案与预期方法路线

## 3.1 技术方案

### 一个工程约束决定了方案的形态

评分器用实测对照计算扰动量：`Δ_pred = ŷ − y_ctrl`、`Δ_true = y − y_ctrl`。
但参赛模型在推断时能否读取测试集对照，取决于测试集蛋白质组文件的可用性。
我们注意到该文件与手册「真值标签保留」的表述存在出入，在获得书面澄清前
按保守假设处理（完整披露见 5.3）。

这一处理有一个直接后果：**测试集的水对照与溶剂对照正位于该文件内，
因此推断时拿不到对照**。凡是形如 `ŷ = y_ctrl + Δ̂` 的模型——
无论其响应模块多精巧——都无法产出合法提交。

我们据此确定方案形态：

```
ŷ = b(实验条件, 测量上下文) + γ · Δ(化合物, 上下文)
```

其中 `b` 是**条件性绝对状态骨架**，只吃元数据；`Δ` 是**强收缩的扰动响应残差**；
`γ` 由验证决定。整个预测路径不接触任何对照。

### 为什么这样拆，而不是端到端

除了可提交性，这个拆分还有一个来自评分定义的理由。

由于评分器两侧减去同一条实测对照，`Δ_pred` 与 `Δ_true` 之差恰为 `(y − b)`，
即模型的**绝对预测误差**。也就是说，`b` 预测得越准，扰动类指标自动越高。
放弃读取对照并未损失分数，反而把优化目标推回了唯一未被共享参照影响的
绝对保真度上——这是我们主动选择而非被迫接受的形态。

分阶段而非端到端训练还可防止基线丰度、批次效应与共享应激被错误吸收进
药物特异模块。

### 输出侧采用低秩表示

`b` 的输出不逐蛋白独立回归，而是先压到低维潜空间再解码。

实测支持：低秩（K₀=16）相对逐蛋白独立 ridge，六项加权总分提升 **+0.0287**，
**配对 bootstrap 95% 区间 [+0.0211, +0.0346]，排除 0**
（按 46 个化合物分组、60 次重抽样，100% 的重抽样中低秩更优）。

我们认为低秩在这里起的是**正则化**作用而非压缩：输入侧的元数据自由度本就只有
二十余维，输出侧却有 5,243 维，逐蛋白独立回归等于允许每个蛋白自行其是；
先经低秩瓶颈迫使各蛋白共享统计强度。这与我们在 2.1 中的观察一致——
化合物平均响应矩阵的有效维度只有两位数，响应本身就位于低维流形上。

秩的选择必须使用**端到端预测**口径：若用"观察留出样本一半蛋白、重建另一半"的
插补口径，会选出 K₀ = 96；而端到端口径选出 K₀ = 16。两者衡量的是不同的问题——
真实预测时看不到留出样本的任何蛋白值。

### 不做不受约束的批次校正

常规组学分析在面对批次效应时会施加 ComBat 之类的全局校正。**本数据不能这样做。**

培养板与培养基、温度、扰动时间的 Cramér's V 均为 **0.99**——一块培养板基本就对应
一个（培养基, 温度, 时间）组合；生物与技术变量合并的 one-hot 设计矩阵 296 列、
秩 267，**亏秩 29**，存在完全别名。在这种结构下，按板号做无约束的全局残差化，
会把这三个生物变量一并删除。

需要准确表述的是：我们主张的是**不做不受约束的全局板效应删除**，
而非"不做任何批次处理"。可用且我们采用的手段包括：手册规定的七项键匹配对照、
可识别的技术协变量、强收缩随机效应。实测显示匹配对照本身已完成大部分批次消除——
响应空间中技术变量的方差解释率降至 0.05–0.27。

唯一残留较强的技术变量是**制备孔位**，它不在手册的七项匹配键内，
匹配对照消不掉，因此作为强收缩随机效应进入模型，未见水平回退为零。

### 缺失值处理

缺失率 27.35%，且强烈符合丰度依赖的检出限（详见 4.1.3）。处理原则有三条：

1. **不填补**。目标缺失位置不当作已知值，损失只作用于可观测位置
2. **不当固定掩码**。同生物条件重复样本的缺失不一致率中位 13.1%，
   说明缺失有显著的逐样本随机成分
3. **不拟合训练集的掩码模式**。因测试集蛋白质组自隔离，无法确认两侧掩码同分布，
   故让模型对掩码模式不敏感，而非去学它

### 泄漏守卫

由于本方案的核心主张之一是"推断不接触对照"，我们为其配了运行时检查：
在模型所声明的全部条件变量上完全相同的样本之间置换对照，
比较预测与自身对照、与他人对照的相关性；若前者显著更高，即判定携带了样本特异信息。

该守卫本身先经**阳性对照**验证：用一个已知读取了对照的模型去测，确认能被拦下，
才用于检验候选模型。我们认为一个抓不到已知泄漏的守卫没有资格宣称别的模型干净。

## 3.2 预期方法路线

### 路线的三个阶段

**第一阶段·先建可信的尺子。** 复刻官方六项指标评分器、建立对照匹配管线，
两者均附单元测试（现为 61 项与 28 项）。理由是本任务评分口径复杂、官方多处未定义，
不先复刻评分器，后续任何"改进"都无法判断真伪。该阶段已完成，
且在过程中捕获了三类会系统性改变模型排名的实现缺陷（见 4.1.1）。

**第二阶段·用基线阶梯与严格留出确定各模块的边际价值。** 已完成。
所有化合物侧结论均在整化合物留出下复核，配随机标签对照与按化合物分组的 bootstrap。

**第三阶段·只保留在不确定性之外仍成立的组件。** 进行中。

### 资源配置的依据

我们用两个神谕基线（使用留出对象自身真值，非合法提交，仅用于定上限）
来量化各条路线的头部空间：

- 直接使用留出化合物自身的平均响应作预测，扰动指标增益 **+0.0617**。
  这是"上下文不变、每化合物一条固定模板"这一**受限模型类**的参照值。
- 绝对状态骨架从逐蛋白 ridge 换为低秩表示，总分一步增益 **+0.0287**，且尚未探顶。

⚠ 这两个数**基线不同、评分模块不同，不可做比值**，我们不宣称任何倍数关系。
可以判定的是：**在当前实现与验证设置下，已实证的最大边际增益更多来自基础状态建模。**
因此剩余资源以绝对状态骨架为主线，化合物特异模块保留为低成本、强收缩的辅助分支。

### 已排除的路线

**基于分子结构的化合物表示**，在严格的整化合物留出下未通过检验：
Morgan 指纹加物化描述符相对"仅上下文"基线的增益为 **−0.0004**，
落在 5 次随机标签对照（+0.0001 ~ +0.0012）之下。

该否定具备效力，因为阳性对照通过了：神谕挑近邻的增益为 **+0.0237**，
说明架构可用、检验有功效；内层选出的正则强度分布在 60–2000，模块未被压制为零。

准确的结论范围是：**当前的二维指纹与最近邻基线，不足以定位响应相似的化合物。**
我们不将其升级为"分子结构预测不了响应"——可能的失效原因至少包括化合物数量、
化学空间覆盖、仅用二维指纹、作用浓度、摄取与外排、代谢、酵母中不存在的靶点、
多靶点与共享应激。

### 后续计划与预注册的保留门槛

为避免看到验证分数后再调整叙事，我们预先规定各模块的保留标准：

| 待验证模块 | 保留门槛 |
|---|---|
| 机制（MoA）类别表示 | 相对"仅上下文"基线，在整化合物留出下的增益须超过随机标签对照分布的上端；类别粒度控制在 6–10 个宽类，一个类别少于 3 个结构独立的训练化合物则不单独训练 |
| 化合物特异残差项 | 分组 bootstrap 的差值区间须排除 0；否则权重设为 0 |
| 未见菌株的控制适配器 | 须在留一菌株验证中稳定超过总体先验；否则删除 |
| 任何复杂度增量 | 须在**同一评分器、同一划分**下超过简单基线，且配对差值区间排除 0 |

**未见菌株**（占测试集 50.1%、无基因型信息）在分支 A 下取训练菌株的总体先验。
我们明确声明这是信息上限而非模型能力问题：在零外部信息下，
存在多个不同的菌株特异响应函数都与已见菌株的全部观测相容。
模型仍可预测培养基、温度、时间的调制以及跨菌株共享的响应分量。

**下一步的技术优先级**依次为：将制备孔位作为强收缩随机效应纳入绝对骨架；
提高分组 bootstrap 的重抽样次数并覆盖全部结论；建立机制类别标注并按上表门槛检验；
产出并校验完整的提交文件。

## 3.3 数据来源、依赖工具与运行流程

**数据来源**

仅使用组委会提供的赛事数据：`metadata_train_val.csv`（8,958 × 15）与
`proteome_raw_train_val.csv`（8,958 样本 × 5,243 蛋白）。测试侧仅读取
`metadata_test.csv`（4,454 × 15）。关于 `proteome_raw_test.csv` 的完整披露见 5.3。

外部数据仅一项：PubChem 公开转储（`CID-Synonym-filtered.gz` 与 `CID-SMILES.gz`），
在本机离线解析化合物名称到结构，用于评估化学结构表示。
未向任何外部服务发送化合物名称或任何赛事数据。该表的使用状态见 5.3。

**依赖工具**

Python 3.13；numpy / pandas / scipy / scikit-learn / RDKit / matplotlib。
无 GPU 依赖，无分布式需求，无商业 API 或闭源模型调用。

**运行流程**

```
metadata + proteome (train_val)
   ↓ ① 数据管线：log2 变换、保留缺失掩码（不填补）
   ↓ ② 对照匹配：手册规定的 7 项键 + Water/DMSO，逐蛋白中位数聚合
   ↓ ③ 诊断：缺失机制 / 批次混杂 / 复制一致性 / 共享参照
   ↓ ④ 基线阶梯 + C-free 可部署骨架（含泄漏守卫）
   ↓ ⑤ 严格验证：官方四类 val + 整化合物留出 + 分组 bootstrap
   ↓
prediction.csv（4,454 × 5,243，样本 ID 不修改）
```

两条关键工程约束：其一，所有参照统计（均值、低秩基、标准化参数）
**仅由训练折计算并冻结**；其二，预测函数的签名与数据流中**不存在测试集对照**，
并由运行时守卫检查——该守卫先用一个已知读取了对照的模型做阳性对照，
确认能拦下已知泄漏后，才用于检验候选模型。

---

# 四、阶段性实验结果或可行性验证

## 4.1 阶段性实验或可行性验证

我们没有直接开始建模，而是先用约一半时间建立**可信的测量与验证基础**。
理由是：本任务六项评分指标口径复杂且官方存在多处未定义项，
若不先复刻评分器，后续任何"改进"都无法判断真伪。

### 4.1.1 复刻官方评分器

按手册实现全部六项指标，并将官方未明确的口径**全部参数化**而非写死：
多对照合并方式、常数向量导致 PCC 未定义时的处理、整条聚合轴未定义时的处理、
未定义模块如何进入总分、样本轴与蛋白轴的组合方式、DEP 阈值与 K。

评分器附 **61 项单元测试**，覆盖完美预测、常数预测、缩放预测、缺失掩码、
多对照合并可切换、向量化实现与逐条参考实现的等价性、float32 落盘往返。

这套测试在开发中捕获了三类会**系统性改变模型排名**的实现缺陷，摘录两例：

- **数值相消**：一遍法计算方差，在 log2 丰度（均值约 20、离差约 2.8）这类
  非中心化数据上发生灾难性相消，把常数向量的方差算成约 1e-11 而非 0，
  于是"常数向量 → PCC 未定义"的判据失效，返回纯由舍入误差决定的**假相关**。
- **未定义项的静默重归一化**：把未定义的指标或聚合轴直接踢出分母，
  会**奖励**"让指标变成未定义"的退化模型——预测恒等于对照的模型使 FC 变成常数向量，
  若将其踢出分母，它反能凭绝对保真度排到第一。

### 4.1.2 数据管线与对照匹配

按手册规定的**七项**匹配键（来源/菌株/培养基/温度/时间/仪器/板号，不含孔位）
与 Water+DMSO 对照集合建立匹配管线，附 28 项单元测试。

关键实测：**7,884 个处理样本的 0 匹配率为 0.0%**，其中 92.9% 匹配到多个合法对照
（中位 2 个）。官方未定义多对照合并规则，我们默认逐蛋白中位数并做成可切换参数。
Quality Control 样本语义上是质控而非生物学对照，单独识别，不并入对照集合。

### 4.1.3 三项诊断实验

| 诊断 | 方法 | 结论 |
|---|---|---|
| 缺失机制 | 缺失率对丰度十分位；缺失指示回归；重复样本缺失不一致率；缺失矩阵主成分 | 强烈符合丰度依赖的检出限（左删失），并叠加技术与随机成分 |
| 测量一致性 | 同化合物 + 同生物条件、跨批次的复制对（4,384 对） | 绝对丰度一致性高，药物响应一致性极低 |
| 批次混杂 | 生物 × 技术的 Cramér's V；设计矩阵秩；对照样本上的方差解释率 | 批次与培养条件近乎完全别名，不可做无约束的全局校正 |

缺失机制的关键数值（图 F2）：Spearman(蛋白中位丰度, 缺失率) = **−0.859**；
按丰度分十档后，**最低丰度档缺失 77.4%，最高丰度档仅 1.6%**；
缺失指示回归中，仅"该蛋白平均有多丰"这一个变量即解释 35.1%，
而全部技术与生物变量合计仅解释 0.4%。

⚠ 表述边界：这**强烈符合**丰度依赖的左删失，但仅凭观测数据无法**完整识别**缺失机制，
故不宣称"已证明为 MNAR"。工程上的处理是：缺失全程保留掩码、不填补，
损失只作用于可观测位置；同生物条件重复样本的缺失不一致率中位 13.1%，
说明缺失也**不能**当作固定掩码处理。

### 4.1.4 基线阶梯与严格外部验证

建立基线阶梯，并追加两类**神谕基线**（使用留出对象自身真值，
不是合法提交，仅用于给某类方法定上限）。

所有化合物侧结论均在**整化合物留出（8 折覆盖全部 43 个化合物）**下复核，
配 shuffled-label 对照与**按化合物分组的 bootstrap**。
**绝不使用随机行划分**——同一化合物在多个批次有重复样本，
随机划分会让同化合物同条件的样本同时出现在训练与验证两侧，分数会虚高。

## 4.2 当前结果

本节数字由 `scripts/scorer/authoritative_results.py` 现场重算并冻结口径指纹
（划分、对照聚合、缺失规则、代码 SHA、随机种子），完整表见 `results/AUTHORITATIVE.md`。
**该分数不是官方分数**——官方多处口径未定，换一组参数分数会变；
其可靠用法是纵向比较自身版本。

### 4.2.1 评分口径的三个实测发现

**发现一：指标 2 的分数大部分来自"共享参照"，而非药物知识。**（图 F1）

评分定义为 `Δ_pred = ŷ − y_ctrl`、`Δ_true = y − y_ctrl`，两侧减去**同一条**实测对照。
因此即使预测完全不含药物信息，其中残留的 `−y_ctrl` 仍会与真值中的同一项对上。

用零药物知识的预测（训练集全局均值谱）在官方四类 val 的 2,806 个处理样本上，
按三种对照配法评估：

| 条件 | 样本轴 PCC | 蛋白轴 PCC |
|---|---|---|
| 正确匹配（官方口径） | 0.1845 | 0.1898 |
| 错配到同实验条件的别的样本 | 0.0825 | 0.0855 |
| 全局错配到随机样本 | 0.0076 | 0.0038 |

**可判定**：共享参照诱导的协方差显著抬高了当前相关指标下的**零知识基线**，
因此**指标 2 不可单独作为模型选择依据**；同一机制也部分影响指标 3 与指标 4。

**不可判定**：三条件之差**不能**拆成可相加的信号份额——相关系数不具可加分解性，
错配同时改变了协方差与分母。要做份额分解，需用独立对照重复分别构造预测与真值的
变化量，并分别报告协方差、方差与相关系数。**该实验我们尚未完成，故不作此声明。**

**这不是评分实现的错误，也不构成取巧空间**——它是评分定义本身的性质。
我们的应对不是去利用它，而是把优化目标放在唯一未受影响的绝对保真度上。

**发现二：药物特异响应的跨批次一致性很低。**（图 F3）

取同化合物、同菌株/培养基/温度/时间、但板号或数据来源不同的复制对（4,384 对）：

| 空间 | 一致性 ρ |
|---|---|
| 绝对 log2 丰度（指标 1，20%） | 0.921 |
| Δ_true 匹配 FC（指标 2，25%） | **0.115** |
| Δ − μ_ctx（指标 3，20%） | 0.157 |
| Δ − μ_drug（指标 4，20%） | **0.059** |

**可判定**：在当前复制对定义下，响应谱的线性一致性很低，
说明药物特异模块的信噪比天然弱、复杂模型极易过拟合。
监督信号应尽量使用同条件多样本聚合后的响应，而非单样本响应。

**不可判定**：该量的准确名称是「跨板/来源复制对的操作性响应谱一致性」，
**不能**解释为"约一成是真信号"，也**不能**由其平方根推出评分上限——
经典可靠性上限要求两次测量对应完全相同的潜在状态、误差独立同方差；
而我们的复制对刻意跨板/跨来源，而板、来源、仪器恰是模型可见的输入，
那部分差异可能是**可预测的系统效应**。

**发现三：批次与培养条件近乎完全别名。**（图 F6）

| 变量对 | Cramér's V |
|---|---|
| 培养基 × 培养板 | 0.99 |
| 温度 × 培养板 | 0.99 |
| 扰动时间 × 培养板 | 0.99 |

生物 + 技术 one-hot 设计矩阵 296 列、秩 267，**亏秩 29**。

**可判定**：**不能对板号做不受约束的全局残差化**；只能使用匹配对照、
可识别的技术协变量、部分池化与强收缩。

**不可判定**：仅在对照样本上做主成分分析时，第一主成分在板号上的方差解释率极高，
但在板号与培养条件近乎共线的前提下，这**不能**证明该主轴是"纯技术"的——
无药物不等于无培养条件差异，多个共线变量都会得到极高解释率。
准确的描述是"与板号、来源、仪器及其别名培养条件共同对齐的主轴"。

匹配对照之后，响应空间中技术变量的方差解释率已降至 0.05–0.27，
说明**匹配对照本身已完成大部分批次消除**。唯一残留较强的是**制备孔位**——
它不在手册规定的七项匹配键内，匹配对照消不掉，应作为强收缩随机效应进入模型。

### 4.2.2 一个决定模型形态的约束：推断时没有测试对照

评分器使用实测对照计算变化量，但**参赛模型在推断时能否读取测试集对照，
取决于测试集蛋白质组的可用性**。我们注意到该文件与手册中「真值标签保留」的表述
存在出入，在获得组委会书面澄清前采取保守处理（完整披露见 5.3）。

其直接后果是：形如 `ŷ = y_ctrl + Δ̂` 的模型**无法用于提交**，
因为测试集的 Water/DMSO 对照正位于该文件内。我们把这类模型全部降级为诊断基线，
另建不依赖对照的可部署形态，并附泄漏守卫。

### 4.2.3 当前水位（图 F4）

**需读测试对照者（oracle 诊断，不可提交）**

| 模型 | 六项加权总分 |
|---|---|
| 预测 = 匹配对照（响应恒为 0） | 0.2836 |
| 总体平均响应 | 0.3087 |
| 上下文均值响应 | 0.3111 |
| 化学近邻响应 | 0.2898 |
| ridge 响应 | 0.3381 |
| 神谕近邻响应（作弊） | 0.3220 |
| 对照向均值收缩（α=0.15） | 0.3843 |

**C-free（推断不接触对照，可提交）**

| 模型 | total | abs_pcc | abs_r2 | fc_pcc |
|---|---|---|---|---|
| 全局均值谱 | 0.2587 | 0.4768 | 0.4138 | 0.1872 |
| 逐蛋白 ridge（满秩） | 0.4406 | 0.8947 | 0.7088 | 0.3052 |
| **低秩 K₀=16 + metadata ridge** | **0.4694** | 0.8984 | 0.7985 | 0.3295 |

三条结构性结论：

1. **可提交模型的分数高于不可提交的 oracle 基线。**
   原因是 `Δ_pred = b − y_ctrl` 与 `Δ_true = y − y_ctrl` 之差恰为 `(y − b)`，
   即模型的绝对预测误差——绝对预测越准，扰动类指标自动越高。
   放弃读取对照并未损失共享参照带来的分数，反而把优化目标推回了绝对保真度。

2. **输出侧低秩表示相对逐蛋白独立回归带来 +0.0287 总分**，
   **配对 bootstrap 95% 区间 [+0.0211, +0.0346]，排除 0**
   （按 46 个化合物分组，60 次重抽样，100% 的重抽样中低秩更优）。
   ⚠ 判据说明：两个模型跑在同一批数据与同一批重抽样上，估计高度相关，
   因此使用**配对**差值的区间，而非比较两条边际区间是否重叠——
   本例中两条边际区间明显重叠，若据此判断会得出相反结论。

3. **秩的选择必须使用端到端预测口径**：用"观察留出样本一半蛋白、重建另一半"的
   插补口径会选出 K₀ = 96，而端到端口径选出 K₀ = 16。两者衡量的是不同的问题——
   真实预测时看不到留出样本的任何蛋白值。

### 4.2.4 已排除的技术路线及其依据（图 F5）

**化学结构表示**：在 8 折整化合物留出下，Morgan 指纹（半径 2）+ RDKit 物化描述符
相对"仅上下文"基线的增益为 **−0.0004**，而 5 次 shuffled-label 对照的增益为
+0.0001 至 +0.0012。即真实结构表示的表现落在随机指派水平之下。

方法学控制包括：外层整化合物留出、化合物等权（样本最多的化合物有 699 行、
最少的仅 30 行，按行拟合会使前者权重是后者的 23 倍）、指纹位过滤与描述符标准化
仅在外层训练折内拟合、正则强度由内层二次留出选择。

**这一否定具备效力，因为阳性对照通过了**：用留出化合物自身真值挑选最相似训练化合物
并照搬其响应（作弊），增益为 +0.0237；内层选出的正则强度分布在 60–2000 之间，
说明响应模块未被正则化压制为零。

**可判定**：**当前的 2D 指纹与最近邻基线，不足以定位响应相似的化合物。**

**不可判定**：不能升级为"分子结构预测不了响应"。可能的失效原因至少包括
化合物数量、化学空间覆盖、仅用 2D 指纹、作用浓度、摄取与外排、代谢、
酵母中不存在的靶点、多靶点与共享应激。神谕近邻使用了留出化合物的真值，
它证明的是**响应空间中存在可迁移的近邻**，不证明结构→响应的映射可学。

**受限模型类的参照值**：直接使用留出化合物自身的平均响应作为预测（完全作弊），
增益为 +0.0617。这是"上下文不变、每化合物一条固定模板"这一**受限模型类**的参照。
⚠ 它与低秩基线的 +0.0287 **基线不同、评分模块不同，不可做比值**。
可以说的是：在当前实现与验证设置下，已实证的最大边际增益更多来自基础状态建模。

### 4.2.5 当前尚未完成的部分

为便于评审判断成熟度，明确列出已知缺口：

1. 分组 bootstrap 已实现并用于核心对比，但尚未覆盖全部结论；
   重抽样次数（60）在正式报告前应提高
2. 机制（MoA）标注尚未建立，其整化合物留出验证台已就绪但未执行
3. 未见菌株目前取训练菌株的总体先验，控制适配器路线尚未验证
4. 因测试集蛋白质组自隔离，无法比较训练与测试的缺失结构是否同分布；
   缓解方式是使模型对掩码模式不敏感，而非拟合训练集的掩码
5. 尚未产出完整的 `prediction.csv` 端到端产物

---

# 五、复现与开放计划

## 5.1 复现方式

**运行环境**：Python 3.13，依赖仅 numpy / pandas / scipy / scikit-learn / RDKit / matplotlib，
无 GPU 依赖，无分布式需求。

**随机性控制**：所有随机过程（复制对抽样、留出折划分、shuffled-label 对照、
随机投影、randomized SVD、bootstrap 重抽样）均使用固定种子。

**数字的单一来源**：所有对外引用的数值由 `scripts/scorer/authoritative_results.py`
现场重算生成，并附口径指纹（划分、对照聚合、缺失规则、代码 SHA、git HEAD、种子）。
文档只引用不复制，并配自动扫描器检查是否有已作废的数值仍在流通。

**验证入口**：

| 命令 | 作用 | 期望 |
|---|---|---|
| `python scripts/scorer/test_metrics.py` | 评分器单元测试 | 61/61 通过 |
| `python scripts/data/test_control_match.py` | 对照匹配单元测试 | 28/28 通过 |
| `python scripts/scorer/authoritative_results.py` | 重算权威数字表 | 生成 `results/AUTHORITATIVE.md` |
| `python scripts/data/stale_number_scan.py` | 检查作废数字是否仍在流通 | 0 处 |
| `python scripts/data/doc_number_check.py` | 核对本文数值与权威表一致 | 全部对上 |
| `python scripts/data/control_match.py` | 对照匹配与响应量产出 | 0 匹配率 0.0% |
| `python scripts/audit/*.py` | 诊断与共享参照实验 | 见 `results/step3_diagnostics/` |
| `python scripts/models/baseline_cfree.py` | C-free 可部署骨架 | 含泄漏守卫检查 |
| `python scripts/models/select_k0.py` | 端到端选秩 | K₀ 曲线 |
| `python scripts/models/loco_response.py` | 整化合物留出 + 对照实验 | 见 `results/step9_loco/` |
| `python scripts/figures/make_figures.py` | 本文全部图 | `results/figures/` |

每个脚本均将完整运行日志写入 `results/` 下对应目录，可与本文逐项核对。

## 5.2 开源计划

**开源范围**：评分器复刻与其 61 项测试、数据管线与其 28 项测试、诊断脚本、
基线与模型代码、出图脚本、权威结果表生成脚本与作废数字扫描器、全部运行日志。

**许可证**：MIT。选择理由是本项目产出以方法与评测工具为主，
宽松许可最有利于其他参赛者与后续研究直接复用；若组委会对赛事产出另有许可要求，
以组委会规定为准。

**公开时间**：按赛事安排，复赛提交时随代码包一并公开完整仓库。
其中**评分器复刻与其测试**这一部分对本赛题其他参与者具有独立价值——
尤其是共享参照效应与未定义项处理的部分——我们愿在组委会认可的前提下提前公开该子集。

**边界**：不开源任何原始赛事数据及其派生的数值文件，受《选手参赛协议》第八条约束。
仓库已配置忽略规则，数据目录不进入版本控制；外发材料另设自动泄漏扫描。

> 【需确认】仓库地址：待你提供 GitHub/Gitee 账号后填入。

## 5.3 依赖、数据来源与合规披露

**赛事数据**：仅使用 `metadata_train_val.csv` 与 `proteome_raw_train_val.csv`；
测试侧仅读取 `metadata_test.csv`。

关于 `proteome_raw_test.csv`，为免歧义，完整披露其读取历史：

- **隔离决定作出之前**，我们对该文件做过一次探查性读取。**正是这次读取发现了
  该文件包含完整蛋白丰度**，与手册「真值标签保留」的表述存在出入——
  这一发现本身就是我们随后决定隔离该文件的依据。该次读取产出的统计
  （总缺失率、每行非空蛋白数、全缺失列数）记录在数据画像文档中。
- **隔离决定作出之后**，该文件未再被读取。代码中不提供其路径常量，
  任何试图打开它的调用会抛出异常；当初那个探查脚本已加运行守卫封存。
- 该文件**从未**进入任何归一化、主成分、超参数选择、模型训练或预测环节。

我们认为这一披露方式比笼统声称「从未读取」更准确：
不先看一眼，无法发现问题，也就不会有隔离决定。

**外部数据**：为评估化学结构表示，使用 PubChem 公开转储
（`CID-Synonym-filtered.gz` 与 `CID-SMILES.gz`）在本机离线解析化合物名称到 SMILES。
**未向任何外部服务发送化合物名称或任何赛事数据。**
解析结果 54/54，其中 4 条为名称别名（两个缩写、两个同系物混合物取主组分），
已在结果表中标注来源等级。

**该外部数据的使用状态**：按 4.2.4 的验证结果，化学结构表示未通过 shuffled-label 对照，
**不进入最终模型**。若最终方案确实不含结构特征，本项属"评估过但未采用"，
按封闭数据榜口径提交；若后续采用，将完整披露并按开放知识榜口径申报。

**商业 API 与闭源模型**：未使用。

**外发材料的合规控制**：任何对外材料均经过自动化泄漏扫描——
从本机数据读出受保护词表逐文件核验，确认不含蛋白丰度数值、
完整化合物名单与菌株代号。

---

# 六、团队介绍

## 6.1 成员背景

**潘兴明**（Pan Xingming），中国药科大学中药学院，在站博士后。

专业方向为天然产物生物合成与酶工程，具体包括萜类合酶的定向进化与半理性设计、
天然产物生物合成途径解析。合作导师董廖斌。

与本赛题相关的核心技能：

- **实验生物学背景**：熟悉酵母作为底盘生物的培养与操作，
  对本题涉及的化合物（含多个萜类天然产物）在真核细胞中的作用机制有直接判断力
- **蛋白质与酶学**：蛋白结构-功能关系、突变位点设计与验证，
  对"结构相似性能否外推到功能相似性"这一问题有来自酶工程的直接经验
- **计算与数据分析**：Python 数据管线、统计建模、科研可视化；
  分子对接与分子动力学模拟

> 【需确认】若为多人参赛，请补充其余成员的单位、岗位与核心技能。

## 6.2 团队分工

本项目为**个人参赛**，全部环节由参赛者本人负责：科学问题界定与技术路线决策、
数据探查与诊断、评分器复刻与验证设计、模型实现与实验、结果解读与文档撰写。

> 【需确认】若实为多人，请按"科学问题界定 / 建模实现 / 验证与复现 / 文档"四类角色分配。

## 6.3 团队成果

> 【需确认】此处需你提供：与本方向相关的过往项目经历、发表成果或获奖经历，
> 以及作品合集链接（如有）。
>
> 建议优先列出与"天然产物机制解析""酶工程定向进化""组学数据分析"相关的条目——
> 它们直接支撑第二节中关于化合物机制与结构-功能关系的判断。
> 我无法代为填写这一项，因为这些是只有你掌握的事实。

---

> **【定稿清单 · 提交前逐项打勾后删除本段】**
>
> - [x] 2.1 / 2.2 已成文（本人口述、整理成文，立场未改）
> - [x] 3.1 / 3.2 已成文
> - [ ] 1.1 项目名称已确认（或改用备选）
> - [ ] 1.3 方案概述已与二、三节的实际内容对齐
> - [ ] 5.2 仓库地址已填
> - [ ] 6.1 成员信息已核对；6.3 过往成果已填
> - [ ] 4.2 各处数字已对照 `results/AUTHORITATIVE.md` 现场核对
> - [ ] 六张图已插入（建议用 `results/figures/*.pdf` 矢量版）
> - [ ] 全文删除所有 `【需确认】` 与 `【状态说明】` 引用块
> - [ ] 确认提交格式与渠道
