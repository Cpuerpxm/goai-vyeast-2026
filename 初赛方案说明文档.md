---
title: "GOAI世界人工智能开源大赛 · 赛道三AI for Research"
subtitle: "算法赛初赛方案说明文档"
---

# 一、项目概述

## 1.1项目名称

不依赖测试对照的虚拟酵母扰动蛋白质组预测：以可提交性与可验证性为先的方案


## 1.2参赛方向

赛道三AI for Research · 算法赛题 · 方向一：虚拟细胞（虚拟酵母扰动响应预测）

主榜任务：使用组委会提供的train_val数据，学习跨菌株、化学扰动、培养基、温度、
时间点与测量上下文的蛋白质组响应规律，预测独立测试集样本的完整log2蛋白质组向量。
考察三类泛化：未见化合物（S1）、未见菌株（S2）、双重未知（S3）。

## 1.3方案概述

本方案面向方向一的主榜任务，预测每个测试样本的完整5,243维log2蛋白丰度向量。

技术方案的形态由一个工程约束反推而来：评分器用实测对照计算扰动量，
但模型在推断时能否读取测试集对照，取决于测试集蛋白质组文件的可用性；
在该文件的状态获得组委会澄清前，我们按保守假设处理（见5.3），
因此模型必须不依赖测试对照即可产出预测。据此确定的形态是

```
ŷ = b(实验条件与测量上下文) + γ · Δ(化合物, 上下文)
```

其中 `b` 是条件性绝对状态骨架，输出侧采用低秩表示；`Δ` 是强收缩的扰动响应残差；
`γ` 由验证决定。缺失值全程保留掩码、不填补，损失只作用于可观测位置。

预期方法路线分三步：先建立可信的测量与验证基础（复刻官方六项指标评分器、
建立对照匹配管线，两者均附单元测试），再用基线阶梯与严格的整化合物留出确定
各模块的边际价值，最后只保留那些在分组bootstrap不确定性之外仍成立的组件。
所有参照统计仅由训练折计算并冻结；模型选择一律使用官方划分与整实体留出，
不使用随机行划分。

当前进度：验证基础设施与诊断已完成，C-free可部署骨架已跑通并优于全部
需读对照的诊断基线；化学结构表示经严格留出检验后未通过随机标签对照，已排除出主线。
详见第四节。

---

# 二、科学问题理解

## 2.1科学问题与研究对象

### 任务的形式

本方向要求：给定酿酒酵母在特定菌株、化合物、培养基、温度、时间与测量上下文下的
扰动蛋白质组训练数据，预测独立测试样本的完整5,243维log2蛋白丰度向量，
并考察三类泛化——未见化合物（S1）、未见菌株（S2）、二者叠加（S3）。

这不是一个纯粹的回归拟合问题。它要求模型把"细胞在某个条件下本来是什么状态"
与"某个化合物会把这个状态推向哪里"这两件事分开表示，
否则无法在实体从未出现过的划分上外推。

### 这类任务在领域中的位置

近年提出的AI虚拟细胞构想（Bunne et al., *Cell* 2024）明确列出两项能力：
预测从未在实验室测试过的扰动的响应，以及在具体细胞上下文中预测该响应。
本题的三类泛化划分与这两项能力直接对应。

但我们不认为本题等同于该构想中的任何单一组件。该构想中的"操纵器"是表示到表示的变换，
而本题要直接输出实测蛋白丰度——中间还必须包含蛋白质组解码与测量过程建模。
更准确的定位是：单物种、单模态、bulk的条件性扰动蛋白质组预测基准，
其能力目标与虚拟细胞构想对齐，但不宣称构成一个完整的虚拟细胞。

### 这条学术线索的来处

把本题看作一道新出现的机器学习竞赛题会低估它。它所属的范式——
扰动 → 高维分子签名 → 机制推断——在酵母中已有二十余年历史。

2000年，Hughes等人在 *Cell* 用 300种突变与化学处理建立酵母表达谱图谱，
仅凭模式匹配就指派了多个未知基因的功能、并发现了常用药物dyclonine的新靶点
（*Cell* 2000, 102, 109–126）。此后，酵母凭借其独有的全基因组敲除库发展出
化学基因组学：一个化合物跨数千个缺失株形成的敏感性指纹，被用来反推靶点与通路。

在人类细胞侧，同一范式演化为Connectivity Map与L1000平台：以约一千个landmark
基因的简化表征代替全转录组（平台名即由此而来），其余转录本由计算推断——
原文报告可推断81%的未测转录本——累计发布130万条谱
（Subramanian et al., *Cell* 2017, 171, 1437–1452）。

读出模态上离本题最近的一支，是LINCS的扰动蛋白质组签名库
（90种药 × 6个细胞系；Litichevskiy et al., *Cell Systems* 2018, 6, 424–443）。
但需要说清它与本题的差别：该库测的是简化表征的磷酸化蛋白质组（P100）
与组蛋白修饰（GCP）两类哨兵读出，不是完整的蛋白丰度谱。
本题要求输出的是5,243个蛋白的完整log2丰度向量，
在读出完整度上比上述任何一支都更接近"整张蛋白质组"。

值得指出的是：当年Hughes等人用表达谱图谱破解机制的那个化合物dyclonine，
正在本赛训练集中，共80个样本。 二十六年后，同一个化合物、同一个物种，
读出从转录组换成了蛋白质组。本题是这条线索的最新一站，不是一次孤立的算法比赛。

### 我们认为真正的困难在哪

不在数据量，而在可迁移实体的数量。

数据表面有8,958行，但独立化学实体只有 43个，独立菌株只有 5株；
官方训练划分中的非对照化合物更少，仅 37个。同一化合物在不同批次的多行样本，
只提高对该化合物效应的估计精度，不会变成新的化学监督样本。

我们的判断是：43个化合物太少，不足以学出可外推的化学规律。
这不是对数据质量的抱怨，而是一个有后果的判断——它意味着任何依赖
"从分子结构学到化学空间到响应的映射"的路线，在本数据规模下都缺乏统计支撑；
模型容量必须按43（能进入响应矩阵的更少，只有37）而非8,958来估算；
化合物侧的任何组件都必须在整化合物留出下验证，
并与随机标签对照比较，否则无法排除偶然。

⚠ 这条判断的证据边界要说清楚。我们已经证实的只是：在本数据上，
二维指纹加物化描述符这一具体表示，在整化合物留出下没有跑过随机标签对照（见4.2.4）。
这与上述判断一致，但不等于已经证明"43个化合物学不出任何化学规律"——
后者是一个更强的命题，需要在多种表示、多种归纳偏置下都失败才能支持。
我们把它作为指导资源配置的工作假设使用，而不作为已证结论陈述。

未见菌株的问题性质不同但同样严峻：它占测试集的 50.1%，
且菌株为匿名代号、不提供任何基因型信息。在零外部信息的前提下，
菌株特异的药物响应在统计上不可识别——这是信息上限，不是模型能力问题。
我们认为诚实的做法是明确声明这一边界，而不是用复杂模型制造可预测的假象。

### 一个我们尚未确定的判断

本题把读出从"生长表型"升级到"完整蛋白质组"，名义维度从个位数跃升到5,243。
但高维读出是否等于高机制分辨率，我们认为并不确定。

一方面，化学-遗传指纹虽然每个菌株只测一个适应度值，
但它跨数千个基因型构成的仍是高维指纹，且直接反映功能依赖与遗传脆弱性，
可能更贴近药物的直接靶点。另一方面，蛋白质组测量的是分子状态，
其中可能包含大量下游的共同胁迫响应。

支持这一疑虑的是复制结构：同一化合物、同一生物条件、跨培养板或跨数据来源的
两条响应向量，线性一致性只有 0.115（同一批复制对上，绝对丰度是0.921）。
即在当前复制定义下，药物特异响应中可跨批次重现的成分很弱。

⚠ 这里要避免一个我们自己先犯过的推断错误。化合物平均响应矩阵解释50%方差需3个
方向、95%需27个，看似说明"有效维度只有两位数"，但该矩阵由37个训练化合物按化合物
求均值后中心化得到，秩上限本就是36，与5,243无关。27/36意味着谱是相对平坦的，
因此这组数不能用来论证响应位于低维流形。真正可判定的只有上面那条一致性。

因此我们倾向于认为：蛋白质组读出看得更细，但噪声也更大，
在稳健性上未必优于传统的生长表型读出。

⚠ 需要说明的是，这是一个待检验的判断，不是已证实的结论。
我们只有本数据集的跨板/来源一致性，没有与历史化学基因组学数据在同一一致性定义下
做过配对比较。若要证实，需要在相同的复制结构与相同的一致性指标下直接比对两类读出。
我们把它作为一个明确的、可证伪的假设提出，而不是作为既定事实使用。

这个判断直接影响建模策略：如果读出本身噪声占优，
那么正确的做法不是堆高模型容量去拟合单样本响应，
而是把监督信号建立在聚合后的响应上，并对单样本预测施加强收缩。

## 2.2科学意义

我们认同官方手册的定位：本方向的价值在于为菌株定向改造与优化提供数据支撑，
是"AI预测 → 湿实验验证"闭环中的一环。

以我们所在的天然产物生物合成与酶工程方向为例，这一价值的具体形态是：
当目标是让某个异源途径或某个酶在酵母中获得更高的表达与活性时，
可供调节的变量（培养基、温度、诱导时间、以及各类小分子处理）组合空间很大，
而每一个组合的湿实验验证成本都不低。一个能够预测扰动后完整蛋白质组的模型，
其直接用途是在实验之前对这些条件组合排序——
把有限的实验通量投向最可能改变目标蛋白丰度的那些条件。

这也解释了为什么本题的评分设计中，绝对保真度与扰动响应两类指标必须同时考察：
对改造决策而言，"这株酵母在这个条件下整体是什么状态"与
"加了这个处理之后哪些蛋白会动"同样重要，缺一不可。

需要克制的是对这一价值的过度承诺。基于第四节的实测结果，
我们认为在当前数据规模下，模型能够可靠支撑的是条件层面的排序与筛选，
而不是对某个未见化合物在某个未见菌株中的机制作出可直接采信的预测。
把可预测的边界说清楚，本身就是让这类模型能真正进入实验决策的前提——
一个不声明适用边界的预测，实验人员无法据以分配资源。

# 三、技术方案与预期方法路线

## 3.1技术方案

### 一个保守假设决定了方案的形态

评分器用实测对照计算扰动量：`Δ_pred = ŷ − y_ctrl`、`Δ_true = y − y_ctrl`。
参赛模型在推断时能否读取测试集对照，取决于测试集蛋白质组文件是否可作为输入——
这属于组委会的认定范畴，我们不作判定。在未获认定前，我们采取保守假设：
不将该文件用于任何环节（完整披露见5.3）。

这一假设有一个直接后果：测试集的水对照与溶剂对照正位于该文件内，
因此在本假设下推断时拿不到对照。凡是形如 `ŷ = y_ctrl + Δ̂` 的模型——
无论其响应模块多精巧——都不作为我们的提交路线。

我们认为这个选择在方法学上也独立成立：该文件所含的正是预测目标本身，
以目标去生成对目标的预测在推断上是循环的，与规则如何认定无关。

我们据此确定方案形态：

```
ŷ = b(实验条件, 测量上下文) + γ · Δ(化合物, 上下文)
```

其中 `b` 是条件性绝对状态骨架，只吃元数据；`Δ` 是强收缩的扰动响应残差；
`γ` 由验证决定。整个预测路径不接触任何对照。

### 为什么这样拆，而不是端到端

除了可提交性，这个拆分还有一个来自评分定义的理由。

由于评分器两侧减去同一条实测对照，`Δ_pred` 与 `Δ_true` 之差恰为 `(b − y)`，
即模型的绝对预测误差。这个恒等式说明：对一个不读对照的模型而言，
扰动类指标上唯一能动的量就是绝对预测误差本身，此外没有别的杠杆。

这个恒等式不能推出「绝对越准，扰动指标就越高」。
它约束的是误差向量，而六项指标算的是相关系数——相关系数对每条向量各自
中心化并归一化，因此误差范数变小不保证相关变大；指标3与指标4还要再减去
分组均值，几何关系又变一次。恒等式只保证优化方向唯一，不保证单调。
我们的依据是实测：把优化目标放在绝对保真度上确实拿到了更高的总分（见 4.2.3），
而非由这个恒等式推出。

分阶段而非端到端训练还可防止基线丰度、批次效应与共享应激被错误吸收进
药物特异模块。

### 输出侧采用低秩表示

`b` 的输出不逐蛋白独立回归，而是先压到低维潜空间再解码。

实测支持：低秩（K₀=16）相对逐蛋白独立ridge，六项加权总分提升 +0.0287，
配对bootstrap 95%区间 [+0.0211, +0.0346]，排除0
（按46个化合物分组、60次重抽样，100%的重抽样中低秩更优）。

我们认为低秩在这里起的是正则化作用而非压缩：输入侧的元数据自由度本就只有
二十余维，输出侧却有5,243维，逐蛋白独立回归等于允许每个蛋白自行其是；
先经低秩瓶颈迫使各蛋白共享统计强度。

⚠ 这条理由是对机制的解释，证据只有上面那个实测增益。
我们不援引"响应位于低维流形"作为论据——2.1已说明，由37个化合物的平均响应
矩阵得到的谱不能支撑该主张。K₀本身由端到端验证选出，不由任何维度先验决定。

秩的选择必须使用端到端预测口径：若用"观察留出样本一半蛋白、重建另一半"的
插补口径，会选出K₀ = 96；而端到端口径选出K₀ = 16。两者衡量的是不同的问题——
真实预测时看不到留出样本的任何蛋白值。

### 不做不受约束的批次校正

常规组学分析在面对批次效应时会施加ComBat之类的全局校正。本数据不能这样做。

培养板与培养基、温度、扰动时间的Cramér's V均为 0.99——一块培养板基本就对应
一个（培养基, 温度, 时间）组合；生物与技术变量合并的one-hot设计矩阵296列、
秩267，亏秩29，存在完全别名。在这种结构下，按板号做无约束的全局残差化，
会把这三个生物变量一并删除。

需要准确表述的是：我们主张的是不做不受约束的全局板效应删除，
而非"不做任何批次处理"。可用且我们采用的手段包括：手册规定的七项键匹配对照、
可识别的技术协变量、强收缩随机效应。实测显示匹配对照本身已完成大部分批次消除——
响应空间中技术变量的方差解释率降至0.05–0.27。

唯一残留较强的技术变量是制备孔位（响应空间中残留η² = 0.272），
它不在手册的七项匹配键内，匹配对照消不掉。
计划的处理是作为强收缩随机效应进入绝对骨架、未见水平回退为零；
⚠ 该项尚未实现，当前报告的全部结果都不含它（见3.2下一步与4.2.5）。

### 缺失值处理

缺失率27.35%，且强烈符合丰度依赖的检出限（详见4.1.3）。处理原则有三条：

1. 不填补。目标缺失位置不当作已知值，损失只作用于可观测位置
2. 不当固定掩码。同生物条件重复样本的缺失不一致率中位13.1%，
   说明缺失有显著的逐样本随机成分
3. 不拟合训练集的掩码模式。因测试集蛋白质组自隔离，无法确认两侧掩码同分布，
   故让模型对掩码模式不敏感，而非去学它

### 泄漏守卫

由于本方案的核心主张之一是"推断不接触对照"，我们为其配了运行时检查：
在模型所声明的全部条件变量上完全相同的样本之间置换对照，
比较预测与自身对照、与他人对照的相关性；若前者显著更高，即判定携带了样本特异信息。

该守卫本身先经阳性对照验证：用一个已知读取了对照的模型去测，确认能被拦下，
才用于检验候选模型。我们认为一个抓不到已知泄漏的守卫没有资格宣称别的模型干净。

## 3.2预期方法路线

### 路线的三个阶段

第一阶段·先建可信的尺子。 复刻官方六项指标评分器、建立对照匹配管线，
两者均附单元测试（现为61项与28项）。理由是本任务评分口径复杂、官方多处未定义，
不先复刻评分器，后续任何"改进"都无法判断真伪。该阶段已完成，
且在过程中捕获了三类会系统性改变模型排名的实现缺陷（见4.1.1）。

第二阶段·用基线阶梯与严格留出确定各模块的边际价值。 已完成。
所有化合物侧结论均在整化合物留出下复核，配随机标签对照与按化合物分组的bootstrap。

第三阶段·只保留在不确定性之外仍成立的组件。 进行中。

### 资源配置的依据

我们用两个神谕基线（使用留出对象自身真值，因而不可实现，仅用于定上限）
来量化各条路线的头部空间：

- 直接使用留出化合物自身的平均响应作预测，指标2的fc_pcc增益 +0.0617。
  这是"上下文不变、每化合物一条固定模板"这一受限模型类的参照值。
- 绝对状态骨架从逐蛋白ridge换为低秩表示，六项总分一步增益 +0.0287，且尚未探顶。

⚠ 这两个数不可直接比大小：前者是单一模块（指标2，权重25%）内的相关增益，
后者是六项加权总分的增益，基线也不同。按权重把前者折成总分需假设其余五项不受影响，
而每化合物一条固定模板必然改变指标4（减去药物均值后的残差）的几何，
该假设未经检验。因此我们不用这两个数排路线优先级，也不宣称任何倍数关系。

资源配置实际依据的是两件同口径可判定的事实：
其一，结构表示路线在自身的随机标签对照下未通过（见"已排除的路线"），
即目前手上没有一个可实现的化合物特异表示——神谕上限再高也无从兑现；
其二，低秩表示的 +0.0287 是已实现、且配对bootstrap区间排除0的增益。
一条路有已验证的增量而另一条尚无可用入口，据此把主线放在绝对状态骨架，
化合物特异模块保留为低成本、强收缩的辅助分支。

### 已排除的路线

基于分子结构的化合物表示，在严格的整化合物留出下未通过检验：
Morgan指纹加物化描述符相对"仅上下文"基线的增益为 −0.0004，
落在5次随机标签对照（+0.0001 ~ +0.0012）之下。

该否定具备效力，因为阳性对照通过了：神谕挑近邻的增益为 +0.0237，
说明架构可用、检验有功效；内层选出的正则强度分布在60–2000，模块未被压制为零。

准确的结论范围是：当前的二维指纹与最近邻基线，不足以定位响应相似的化合物。
我们不将其升级为"分子结构预测不了响应"——可能的失效原因至少包括化合物数量、
化学空间覆盖、仅用二维指纹、作用浓度、摄取与外排、代谢、酵母中不存在的靶点、
多靶点与共享应激。

### 后续计划与预注册的保留门槛

为避免看到验证分数后再调整叙事，我们预先规定各模块的保留标准：

| 待验证模块 | 保留门槛 |
|:-------------|:-------------------------------------------------------------------------------|
| 机制（MoA）类别表示 | 相对"仅上下文"基线，在整化合物留出下的增益须超过随机标签对照分布的上端；类别粒度控制在6–10个宽类，一个类别少于3个结构独立的训练化合物则不单独训练 |
| 化合物特异残差项 | 分组bootstrap的差值区间须排除0；否则权重设为0 |
| 未见菌株的控制适配器 | 须在留一菌株验证中稳定超过总体先验；否则删除 |
| 任何复杂度增量 | 须在同一评分器、同一划分下超过简单基线，且配对差值区间排除0 |

未见菌株（占测试集50.1%、无基因型信息）在分支A下取训练菌株的总体先验。
我们明确声明这是信息上限而非模型能力问题：在零外部信息下，
存在多个不同的菌株特异响应函数都与已见菌株的全部观测相容。
模型仍可预测培养基、温度、时间的调制以及跨菌株共享的响应分量。

下一步的技术优先级依次为：将制备孔位作为强收缩随机效应纳入绝对骨架；
提高分组bootstrap的重抽样次数并覆盖全部结论；建立机制类别标注并按上表门槛检验；
产出并校验完整的提交文件。

## 3.3数据来源、依赖工具与运行流程

数据来源

仅使用组委会提供的赛事数据：`metadata_train_val.csv`（8,958 × 15）与
`proteome_raw_train_val.csv`（8,958样本 × 5,243蛋白）。测试侧仅读取
`metadata_test.csv`（4,454 × 15）。关于 `proteome_raw_test.csv` 的完整披露见5.3。

外部数据仅一项：PubChem公开转储（`CID-Synonym-filtered.gz` 与 `CID-SMILES.gz`），
在本机离线解析化合物名称到结构，用于评估化学结构表示。
未向任何外部服务发送化合物名称或任何赛事数据。该表的使用状态见5.3。

依赖工具

Python 3.13；numpy / pandas / scipy / scikit-learn / RDKit / matplotlib。
无GPU依赖，无分布式需求，无商业API或闭源模型调用。

运行流程

```
metadata + proteome (train_val)
   ↓ ① 数据管线：log2 变换、保留缺失掩码（不填补）
   ↓ ② 对照匹配：手册规定的 7 项键 + Water/DMSO，逐蛋白中位数聚合
   ↓ ③ 诊断：缺失机制 / 批次混杂 / 复制一致性 / 共享参照
   ↓ ④ 基线阶梯 + C-free 可部署骨架（含泄漏守卫）
   ↓ ⑤ 严格验证：官方四类 val + 整化合物留出 + 分组 bootstrap
   ↓
prediction.csv（4,454 × 5,243，样本 ID 不修改）
```

两条关键工程约束：其一，所有参照统计（均值、低秩基、标准化参数）
仅由训练折计算并冻结；其二，预测函数的签名与数据流中不存在测试集对照，
并由运行时守卫检查——该守卫先用一个已知读取了对照的模型做阳性对照，
确认能拦下已知泄漏后，才用于检验候选模型。

---

# 四、阶段性实验结果或可行性验证

## 4.1阶段性实验或可行性验证

我们没有直接开始建模，而是先用约一半时间建立可信的测量与验证基础。
理由是：本任务六项评分指标口径复杂且官方存在多处未定义项，
若不先复刻评分器，后续任何"改进"都无法判断真伪。

### 4.1.0实体清点：本文各处化合物计数的口径

本文在不同位置出现37、43、46、54这几个数。它们不是同一个量的不同版本，
而是四个不同口径，各有其用途，先在此一次说清：

| 口径 | 数 | 含义与用在哪 |
|:------------------------------------|:------------------|:--------------------------------------|
| train_val唯一扰动标签 | 46 | 含Water、DMSO、Quality Control三个非化合物标签。按化合物分组的bootstrap即以此为分组键，故报"46组" |
| train_val非对照化合物 | 43 | 46减去上述三个标签。整化合物留出（8折）覆盖的就是这43个 |
| 官方train划分内的非对照化合物 | 37 | 只有它们能进入响应矩阵的行。模型可用的化学监督量以此为准，比43更紧 |
| test唯一非对照化合物 | 54 | 其中11个仅见于test（未见化合物）。SMILES解析的目标集合即这54个 |

由此得到两条会被反复引用的约束：响应矩阵按化合物求均值后共37行，
中心化后秩上限36；而赛题层面的硬上限（train_val全部43个化合物）为42。
本方案实际受制于前者。

### 4.1.1复刻官方评分器

按手册实现全部六项指标，并将官方未明确的口径全部参数化而非写死：
多对照合并方式、常数向量导致PCC未定义时的处理、整条聚合轴未定义时的处理、
未定义模块如何进入总分、样本轴与蛋白轴的组合方式、DEP阈值与K。

评分器附 61项单元测试，覆盖完美预测、常数预测、缩放预测、缺失掩码、
多对照合并可切换、向量化实现与逐条参考实现的等价性、float32落盘往返。

这套测试在开发中捕获了三类会系统性改变模型排名的实现缺陷，摘录两例：

- 数值相消：一遍法计算方差，在log2丰度（均值约20、离差约2.8）这类
  非中心化数据上发生灾难性相消，把常数向量的方差算成约1e-11而非0，
  于是"常数向量 → PCC未定义"的判据失效，返回纯由舍入误差决定的假相关。
- 未定义项的静默重归一化：把未定义的指标或聚合轴直接踢出分母，
  会奖励"让指标变成未定义"的退化模型——预测恒等于对照的模型使FC变成常数向量，
  若将其踢出分母，它反能凭绝对保真度排到第一。

### 4.1.2数据管线与对照匹配

按手册规定的七项匹配键（来源/菌株/培养基/温度/时间/仪器/板号，不含孔位）
与Water+DMSO对照集合建立匹配管线，附28项单元测试。

关键实测：7,884个处理样本的0匹配率为0.0%，其中92.9%匹配到多个合法对照
（中位2个）。官方未定义多对照合并规则，我们默认逐蛋白中位数并做成可切换参数。
Quality Control样本语义上是质控而非生物学对照，单独识别，不并入对照集合。

### 4.1.3三项诊断实验

| 诊断 | 方法 | 结论 |
|:-------------|:---------------------------------------|:--------------------------------------|
| 缺失机制 | 缺失率对丰度十分位；缺失指示回归；重复样本缺失不一致率；缺失矩阵主成分 | 强烈符合丰度依赖的检出限（左删失），并叠加技术与随机成分 |
| 测量一致性 | 同化合物 + 同生物条件、跨批次的复制对（4,384对） | 绝对丰度一致性高，药物响应一致性极低 |
| 批次混杂 | 生物 × 技术的Cramér's V；设计矩阵秩；对照样本上的方差解释率 | 批次与培养条件近乎完全别名，不可做无约束的全局校正 |

![](../results/figures/F2_missing_mnar.png)
图F2　蛋白缺失率随丰度十分位的变化。

缺失机制的关键数值：Spearman(蛋白中位丰度, 缺失率) = −0.859；
按丰度分十档后，最低丰度档缺失77.4%，最高丰度档仅1.6%；
缺失指示回归中，仅"该蛋白平均有多丰"这一个变量即解释35.1%，
而全部技术与生物变量合计仅解释0.4%。

⚠ 表述边界：这强烈符合丰度依赖的左删失，但仅凭观测数据无法完整识别缺失机制，
故不宣称"已证明为MNAR"。工程上的处理是：缺失全程保留掩码、不填补，
损失只作用于可观测位置；同生物条件重复样本的缺失不一致率中位13.1%，
说明缺失也不能当作固定掩码处理。

### 4.1.4基线阶梯与严格外部验证

建立基线阶梯，并追加两类神谕基线（使用留出对象自身真值，
因而不可实现，仅用于给某类方法定上限）。

所有化合物侧结论均在整化合物留出（8折覆盖全部43个化合物）下复核，
配shuffled-label对照与按化合物分组的bootstrap。
绝不使用随机行划分——同一化合物在多个批次有重复样本，
随机划分会让同化合物同条件的样本同时出现在训练与验证两侧，分数会虚高。

## 4.2当前结果

本节数字由 `scripts/scorer/authoritative_results.py` 现场重算并冻结口径指纹
（划分、对照聚合、缺失规则、代码SHA、随机种子），完整表见 `results/AUTHORITATIVE.md`。
该分数不是官方分数——官方多处口径未定，换一组参数分数会变；
其可靠用法是纵向比较自身版本。

### 4.2.1评分口径的三个实测发现

#### 发现一：指标2的分数大部分来自「共享参照」，而非药物知识

![](../results/figures/F1_shared_reference.png)
图F1　零药物知识预测在三种对照配法下的指标2相关系数。

评分定义为 `Δ_pred = ŷ − y_ctrl`、`Δ_true = y − y_ctrl`，两侧减去同一条实测对照。
因此即使预测完全不含药物信息，其中残留的 `−y_ctrl` 仍会与真值中的同一项对上。

用零药物知识的预测（训练集全局均值谱）在官方四类val的2,806个处理样本上，
按三种对照配法评估：

| 条件 | 样本轴PCC | 蛋白轴PCC |
|:--------------------------------------|:---------------------------|:---------------------------|
| 正确匹配（官方口径） | 0.1845 | 0.1898 |
| 错配到同实验条件的别的样本 | 0.0825 | 0.0855 |
| 全局错配到随机样本 | 0.0076 | 0.0038 |

可判定：共享参照诱导的协方差显著抬高了当前相关指标下的零知识基线，
因此指标2不可单独作为模型选择依据。指标3与指标4同样以 `Δ` 为输入，
减去分组均值只能消掉对照项中随该组共变的部分，机制上应残留一部分同源影响；
我们未对该残留量做过测量，故只作机制陈述，不给份额。

不可判定：三条件之差不能拆成可相加的信号份额——相关系数不具可加分解性，
错配同时改变了协方差与分母。要做份额分解，需用独立对照重复分别构造预测与真值的
变化量，并分别报告协方差、方差与相关系数。该实验我们尚未完成，故不作此声明。

这不是评分实现的错误，也不构成取巧空间——它是评分定义本身的性质。
我们的应对不是去利用它，而是把优化目标放在唯一未受影响的绝对保真度上。

#### 发现二：药物特异响应的跨批次一致性很低

![](../results/figures/F3_reliability.png)
图F3　四个指标空间的跨批次复制一致性。

取同化合物、同菌株/培养基/温度/时间、但板号或数据来源不同的复制对（4,384对）：

| 空间 | 一致性 ρ |
|:---------------------------------------------------------------------------------|:-----------|
| 绝对log2丰度（指标1，20%） | 0.921 |
| Δ_true匹配FC（指标2，25%） | 0.115 |
| Δ − μ_ctx（指标3，20%） | 0.157 |
| Δ − μ_drug（指标4，20%） | 0.059 |

可判定：在当前复制对定义下，响应谱的跨板/来源一致性很低，
即药物特异模块中可跨批次重现的成分弱、复杂模型极易过拟合。
监督信号应尽量使用同条件多样本聚合后的响应，而非单样本响应。

不可判定：该量的准确名称是「跨板/来源复制对的操作性响应谱一致性」，
不能解释为"约一成是真信号"，也不能由其平方根推出评分上限——
经典可靠性上限要求两次测量对应完全相同的潜在状态、误差独立同方差；
而我们的复制对刻意跨板/跨来源，而板、来源、仪器恰是模型可见的输入，
那部分差异可能是可预测的系统效应。

#### 发现三：批次与培养条件近乎完全别名

![](../results/figures/F6_confounding.png)
图F6　生物条件与测量上下文的Cramér's V关联矩阵。

| 变量对 | Cramér's V |
|:------------------------------------------------------------------------------|:--------------|
| 培养基 × 培养板 | 0.99 |
| 温度 × 培养板 | 0.99 |
| 扰动时间 × 培养板 | 0.99 |

生物 + 技术one-hot设计矩阵296列、秩267，亏秩29。

可判定：不能对板号做不受约束的全局残差化；只能使用匹配对照、
可识别的技术协变量、部分池化与强收缩。

不可判定：仅在对照样本上做主成分分析时，第一主成分在板号上的方差解释率极高，
但在板号与培养条件近乎共线的前提下，这不能证明该主轴是"纯技术"的——
无药物不等于无培养条件差异，多个共线变量都会得到极高解释率。
准确的描述是"与板号、来源、仪器及其别名培养条件共同对齐的主轴"。

匹配对照之后，响应空间中技术变量的方差解释率已降至0.05–0.27，
说明匹配对照本身已完成大部分批次消除。唯一残留较强的是制备孔位（η² = 0.272）——
它不在手册规定的七项匹配键内，匹配对照消不掉，应作为强收缩随机效应进入模型。
该项目前仍是待办，本节报告的所有分数均未包含它。

### 4.2.2一个决定模型形态的保守假设：不使用测试集对照

评分器使用实测对照计算变化量。组委会发布的数据包中包含测试集蛋白质组文件，
其中含有本任务要求预测的蛋白丰度值。

该文件能否作为模型输入，属于组委会的认定范畴，我们无权判定。
在未获认定前，我们采取保守假设：不将该文件用于任何环节（完整披露见5.3）。

其直接后果是：形如 `ŷ = y_ctrl + Δ̂` 的模型，在本假设下不作为我们的提交路线——
因为测试集的Water/DMSO对照正位于该文件内。我们把这类模型全部降级为诊断基线，
另建不依赖对照的可部署形态，并附泄漏守卫。

表述边界：以上是我们的处理选择，不构成对该类模型合法性的判定。
若组委会认定该文件可作为输入，本方案架构仍然成立，
届时读取对照的分支可作为可插拔模块加入。

### 4.2.3当前水位

![](../results/figures/F4_baseline_ladder.png)
图F4　基线阶梯：可提交模型与需读对照的诊断基线分列。

需读测试对照者（oracle诊断，不可提交）

| 模型 | 六项加权总分 |
|:--------------------------------------------------------------------------------|:------------|
| 预测 = 匹配对照（响应恒为0） | 0.2836 |
| 总体平均响应 | 0.3087 |
| 上下文均值响应 | 0.3111 |
| 化学近邻响应 | 0.2898 |
| ridge响应 | 0.3381 |
| 神谕近邻响应（响应空间上限） | 0.3220 |
| 对照向均值收缩（α=0.15） | 0.3843 |

C-free（推断不接触对照，可提交）

| 模型 | total | abs_pcc | abs_r2 | fc_pcc |
|:---------------------------|:----------------|:-----------------|:----------------|:----------------|
| 全局均值谱 | 0.2587 | 0.4768 | 0.4138 | 0.1872 |
| 逐蛋白ridge满秩 | 0.4406 | 0.8947 | 0.7088 | 0.3052 |
| 低秩K₀=16 | 0.4694 | 0.8984 | 0.7985 | 0.3295 |

三条结构性结论：

1. 可提交模型的分数高于需读对照的诊断基线。
   机制上，`Δ_pred = b − y_ctrl` 与 `Δ_true = y − y_ctrl` 之差恰为 `(b − y)`，
   即绝对预测误差；对不读对照的模型来说，扰动类指标上可动的量只剩它。
   这个恒等式只说明优化方向唯一，不蕴含「绝对越准则扰动指标必然更高」
   （六项指标是相关系数，误差范数与相关不是单调对应，详见 3.1）。
   上表是实测结果，不是由该恒等式推出的。

2. 输出侧低秩表示相对逐蛋白独立回归带来 +0.0287总分，
   配对bootstrap 95%区间 [+0.0211, +0.0346]，排除0
   （按46个化合物分组，60次重抽样，100%的重抽样中低秩更优）。
   ⚠ 判据说明：两个模型跑在同一批数据与同一批重抽样上，估计高度相关，
   因此使用配对差值的区间，而非比较两条边际区间是否重叠——
   本例中两条边际区间明显重叠，若据此判断会得出相反结论。

   ⚠ 这个增益在本文可能以三个略有差别的数出现，它们是同一件事的三种算法，
   不是三次不同的实验，为免误读一次列清：

   | 数 | 怎么来的 | 用在哪 |
|:----------------|:--------------------------------------|:-------------------------------------|
   | +0.0287 | 全精度点估计相减：0.469387 − 0.440649 | 正文统一采用此数 |
   | +0.0288 | 上表四舍五入到四位后相减：0.4694 − 0.4406 | 读者自行核对时会得到，属舍入差 |
   | +0.0282 | 60次配对重抽样的差值均值 | 区间的中心，与点估计不必相等 |

   三者相差在千分之一以内，不改变任何结论。

3. 秩的选择必须使用端到端预测口径：用"观察留出样本一半蛋白、重建另一半"的
   插补口径会选出K₀ = 96，而端到端口径选出K₀ = 16。两者衡量的是不同的问题——
   真实预测时看不到留出样本的任何蛋白值。

分场景表现（同一模型，仅按官方划分拆开看，可看出它在哪类外推上垮）：

| 场景 | 样本数 | 绝对PCC | 绝对R² | FC相关 |
|:---------------------------|---------------:|----------------:|----------------:|----------------:|
| S1 未见化合物 | 1,065 | 0.9212 | 0.8525 | 0.3505 |
| S2 未见菌株 | 1,547 | 0.8865 | 0.7233 | 0.3265 |
| S3 双重未知 | 269 | 0.9125 | 0.7631 | 0.3035 |
| 时间外推 | 157 | 0.9066 | 0.8284 | 0.3506 |
| （参考）样本内 | 5,920 | 0.9043 | 0.8244 | 0.3582 |

场景与官方划分的对应：S1 = `val_chem_only`，S2 = `val_strain_only`，
S3 = `val_both`，时间外推 = `val_time`，样本内 = `train`。
FC相关即指标2的匹配对照原始FC的PCC。

三点可读：其一，未见菌株（S2）是最弱的一环，绝对R²从0.85掉到0.72，
与2.1的判断一致——菌株为匿名代号、无基因型信息，菌株特异响应不可识别；
其二，双重未知（S3）的FC相关最低，两类未知叠加确实更难；
其三，val各划分与train行的分数处在同一水平（train甚至不是最高），
说明模型没有把训练身份背下来，这正是低秩瓶颈与强正则的预期效果。

端到端提交产物：`scripts/models/predict_test.py` 已跑通完整链路，
产出 `prediction.csv`（4,454 × 5,243，首列为未经修改的 `sample_ID`），
并通过全部落盘校验：行列数、无NaN、无Inf、ID顺序与 `metadata_test.csv`
原始行序逐行一致、ID与蛋白名均无重复、预测值域落在训练观测范围内
（[10.99, 32.91] 对 [10.02, 35.30]）。

两处需要说明的工程决定：

- 186个在训练集中无任何观测的蛋白列，没有可用信号，统一取有观测列均值的
  中位数（20.51）作确定性回退。不取0——log2丰度取0意味着"丰度为1"，
  是个明显错误的绝对值。
- 提交模型用全部train_val拟合，而上表的val分数来自仅用train折拟合的模型。
  两者必须分开：val行的标签是已知的，最终提交没有理由丢掉；
  但一旦用它们拟合，val分数就成了样本内，不能再当成绩报告。
  一个具体后果是有一株菌株只出现在 `val_strain_only`，只用train折会把它整株丢掉，
  到测试集上反而变成"未见菌株"。按全量拟合后，测试集中真正未见的只剩一株
  （2,231行，占50.1%）。

### 4.2.4已排除的技术路线及其依据

![](../results/figures/F5_loco_structure.png)
图F5　整化合物留出下，结构表示、随机标签对照与两级神谕基线的增益对比。

化学结构表示：在8折整化合物留出下，Morgan指纹（半径2）+ RDKit物化描述符
相对"仅上下文"基线的增益为 −0.0004，而5次shuffled-label对照的增益为
+0.0001至 +0.0012。即真实结构表示的表现落在随机指派水平之下。

方法学控制包括：外层整化合物留出、化合物等权（样本最多的化合物有699行、
最少的仅30行，按行拟合会使前者权重是后者的23倍）、指纹位过滤与描述符标准化
仅在外层训练折内拟合、正则强度由内层二次留出选择。

这一否定具备效力，因为阳性对照通过了：用留出化合物自身真值挑选最相似训练化合物
并照搬其响应（不可实现，仅用于给响应空间定上限），增益为 +0.0237；
内层选出的正则强度分布在60–2000之间，说明响应模块未被正则化压制为零。

可判定：当前的2D指纹与最近邻基线，不足以定位响应相似的化合物。

不可判定：不能升级为"分子结构预测不了响应"。可能的失效原因至少包括
化合物数量、化学空间覆盖、仅用2D指纹、作用浓度、摄取与外排、代谢、
酵母中不存在的靶点、多靶点与共享应激。神谕近邻使用了留出化合物的真值，
它证明的是响应空间中存在可迁移的近邻，不证明结构→响应的映射可学。

受限模型类的参照值：直接使用留出化合物自身的平均响应作为预测
（同样不可实现，用于给该受限模型类定上限），增益为 +0.0617。
这是"上下文不变、每化合物一条固定模板"这一受限模型类的参照。
⚠ 它与低秩基线的 +0.0287 不在同一口径上：前者是指标2内的fc_pcc增益，
后者是六项加权总分的增益，基线也不同，因此不可直接比大小，也不做比值。
我们据以配置资源的不是这两个数的比较，而是"结构路线未通过自身随机标签对照、
低秩路线的增益已被配对bootstrap确认"这两件同口径事实（详见3.2）。

### 4.2.5当前尚未完成的部分

为便于评审判断成熟度，明确列出已知缺口：

1. 分组bootstrap已实现并用于核心对比，但尚未覆盖全部结论；
   重抽样次数（60）在正式报告前应提高
2. 机制（MoA）标注尚未建立，其整化合物留出验证台已就绪但未执行
3. 未见菌株目前取训练菌株的总体先验，控制适配器路线尚未验证
4. 因测试集蛋白质组自隔离，无法比较训练与测试的缺失结构是否同分布；
   缓解方式是使模型对掩码模式不敏感，而非拟合训练集的掩码
5. 制备孔位的强收缩随机效应尚未接入绝对骨架，本节所有分数均不含它

---

# 五、复现与开放计划

## 5.1复现方式

运行环境：Python 3.13，依赖仅numpy / pandas / scipy / scikit-learn / RDKit / matplotlib，
无GPU依赖，无分布式需求。精确版本号见5.3。

随机性控制：所有随机过程（复制对抽样、留出折划分、shuffled-label对照、
随机投影、randomized SVD、bootstrap重抽样）均使用固定种子。

数字的单一来源：所有对外引用的数值由 `scripts/scorer/authoritative_results.py`
现场重算生成，并附口径指纹（划分、对照聚合、缺失规则、代码SHA、git HEAD、种子）。
文档只引用不复制，并配自动扫描器检查是否有已作废的数值仍在流通。

验证入口：

| 命令 | 作用 | 期望 |
|:-------------------------------|:-------------------------|:------------------------------------|
| `python scripts/scorer/test_metrics.py` | 评分器单元测试 | 61/61通过 |
| `python scripts/data/test_control_match.py` | 对照匹配单元测试 | 28/28通过 |
| `python scripts/scorer/authoritative_results.py` | 重算权威数字表 | 生成 `results/AUTHORITATIVE.md` |
| `python scripts/data/stale_number_scan.py` | 检查作废数字是否仍在流通 | 0处 |
| `python scripts/data/doc_number_check.py` | 反向核对：本文出现的每个数是否都能追到出处 | 退出码0 |
| `python scripts/data/control_match.py` | 对照匹配与响应量产出 | 0匹配率0.0% |
| `python scripts/audit/*.py` | 诊断与共享参照实验 | 见 `results/step3_diagnostics/` |
| `python scripts/models/baseline_cfree.py` | C-free可部署骨架 | 含泄漏守卫检查 |
| `python scripts/models/select_k0.py` | 端到端选秩 | K₀ 曲线 |
| `python scripts/models/loco_response.py` | 整化合物留出 + 对照实验 | 见 `results/step9_loco/` |
| `python scripts/models/predict_test.py` | 端到端产出提交文件 | `results/step10_submission/prediction.csv`，落盘前逐项校验 |
| `python scripts/figures/make_figures.py` | 本文全部图（数值取自权威表） | `results/figures/` |

每个脚本均将完整运行日志写入 `results/` 下对应目录，可与本文逐项核对。

## 5.2开源计划

开源范围：评分器复刻与其61项测试、数据管线与其28项测试、诊断脚本、
基线与模型代码、出图脚本、权威结果表生成脚本与作废数字扫描器、全部运行日志。

许可证：MIT。选择理由是本项目产出以方法与评测工具为主，
宽松许可最有利于其他参赛者与后续研究直接复用；若组委会对赛事产出另有许可要求，
以组委会规定为准。

公开时间：按赛事安排，复赛提交时随代码包一并公开完整仓库。
其中评分器复刻与其测试这一部分对本赛题其他参与者具有独立价值——
尤其是共享参照效应与未定义项处理的部分——我们愿在组委会认可的前提下提前公开该子集。

边界：不开源任何原始赛事数据及其派生的数值文件，受《选手参赛协议》第八条约束。
仓库已配置忽略规则，数据目录不进入版本控制；外发材料另设自动泄漏扫描。

仓库地址：https://github.com/Cpuerpxm/goai-vyeast-2026

当前为私有仓库；按上述时间安排，于复赛提交时转为公开。
仓库内不含任何赛事数据，运行日志已经过自动化脱敏扫描。

## 5.3依赖、数据来源与合规披露

赛事数据：仅使用 `metadata_train_val.csv` 与 `proteome_raw_train_val.csv`；
测试侧仅读取 `metadata_test.csv`。

关于 `proteome_raw_test.csv`，我们完整披露对该文件的全部操作：

事实陈述

- 该文件由组委会随数据包发布，规模 4,454 × 5,243，其中约 72% 的单元格含蛋白丰度数值。
  这些数值即本任务要求预测的目标量。
- 项目起始阶段，我们执行过一次探查性读取，用于核查数据包的整体结构。
  该次读取计算的是聚合统计量：矩阵形状、缺失单元格总数、缺失率、
  每行非空蛋白数、全缺失列数。相应数字记录在我们的数据画像文档中。
- 该次读取之后，我们决定不再使用该文件，并以工程方式落实：代码中不提供其路径常量，
  任何试图打开它的调用会抛出异常；执行该次读取的脚本已加运行守卫封存。
- 此后该文件未再被读取，且从未进入归一化、主成分、超参数选择、
  模型训练或预测的任何环节。

我们的立场

该文件能否作为模型输入，由组委会认定。我们不作判定，
仅说明在未获认定前采取了更严格的处理方式，并将上述操作完整记录在案，
以便复现审核时逐项核验。

运行环境与依赖版本（结果的可复现口径以此为准）：

| 项 | 版本 |
|:------------------|:--------------------------------------------------------------------------|
| 操作系统 | Windows 11（10.0.26200） |
| Python | 3.13.12 |
| numpy | 2.4.4 |
| pandas | 3.0.2 |
| scipy | 1.17.1 |
| matplotlib | 3.10.1 |
| RDKit | 2024.09.5（仅用于已排除的结构表示路线，不在提交路径上） |

除上述外不依赖其他第三方库；不使用GPU专用库，全流程可在CPU上复现。

外部数据：为评估化学结构表示，使用PubChem公开转储在本机离线解析
化合物名称到SMILES。未向任何外部服务发送化合物名称或任何赛事数据——
出去的只有对两个固定公开URL的GET请求。转储文件的取用凭据如下：

| 文件 | 下载日期 | 大小 | SHA-256前16位 |
|:--------------------------------------|:--------------------|:-----------|:----------------------|
| `CID-Synonym-filtered.gz` | 2026-08-05 | 0.96 GB | `c6d57604dc0fe746` |
| `CID-SMILES.gz` | 2026-08-05 | 1.48 GB | `b62cf763f38b0d48` |

来源：`ftp.ncbi.nlm.nih.gov/pubchem/Compound/Extras/`。
PubChem为滚动更新，复现时若哈希不符，说明上游转储已变，
应以本表所列哈希的版本为准；解析脚本在 `scripts/data/resolve_smiles.py`。

解析结果54/54，其中4条为名称别名（两个缩写、两个同系物混合物取主组分），
已在结果表中标注来源等级。

该外部数据的使用状态：按4.2.4的验证结果，化学结构表示未通过shuffled-label对照，
不进入最终模型。

榜单归属由组委会认定，我们不自行判定。 我们如实披露：
在方案开发与路线筛选过程中使用过该公开转储评估化学结构表示，
但最终模型不含由其派生的任何特征。
若榜单归属以"最终模型的输入"为准，本方案不含外部数据；
若以"开发过程中使用过的数据"为准，则本项应计入。
两种口径下的事实均如上，请组委会依规认定。

商业API与闭源模型：未使用。

外发材料的合规控制：任何对外材料均经过自动化泄漏扫描——
从本机数据读出受保护词表逐文件核验，确认不含蛋白丰度数值、
完整化合物名单与菌株代号。

---

# 六、团队介绍

## 6.1成员背景

潘兴明（Pan Xingming），中国药科大学中药学院，在站博士后。

专业方向为天然产物生物合成与酶工程，具体包括萜类合酶的定向进化与半理性设计、
天然产物生物合成途径解析。合作导师董廖斌。

与本赛题相关的核心技能：

- 实验生物学背景：熟悉酵母作为底盘生物的培养与操作，
  对本题涉及的化合物（含多个萜类天然产物）在真核细胞中的作用机制有直接判断力
- 蛋白质与酶学：蛋白结构-功能关系、突变位点设计与验证，
  对"结构相似性能否外推到功能相似性"这一问题有来自酶工程的直接经验
- 计算与数据分析：Python数据管线、统计建模、科研可视化；
  分子对接与分子动力学模拟


## 6.2团队分工

本项目为个人参赛，全部环节由参赛者本人负责：科学问题界定与技术路线决策、
数据探查与诊断、评分器复刻与验证设计、模型实现与实验、结果解读与文档撰写。


## 6.3团队成果

参赛者以第一作者身份发表的代表性成果，均围绕萜类环化酶的结构、机制与工程改造，
与本赛题涉及的酵母底盘、天然产物机制解析和结构-功能关系判断直接相关：

1. Pan X., Du W., Zhang X., Lin X., Li F.-R., Yang Q., Wang H., Rudolf J. D.,
   Zhang B., Dong L.-B. *Discovery, Structure, and Mechanism of a Class II
   Sesquiterpene Cyclase.* J. Am. Chem. Soc. 2022, 144 (48), 22067–22074.
   DOI: 10.1021/jacs.2c09412
   —— 首次发现并解析II类倍半萜环化酶的结构与催化机制。

2. Pan X., Rudolf J. D., Dong L.-B. *Class II terpene cyclases: structures,
   mechanisms, and engineering.* Nat. Prod. Rep. 2024, 41 (3), 402–433.
   DOI: 10.1039/d3np00033h
   —— 系统综述II类萜烯环化酶的结构、机制与工程改造。

3. Pan X., Li H., Dong L.-B. *Engineering class I terpene synthases for skeletal
   diversity: strategies and applications.* Nat. Prod. Rep. 2026, 43 (1), 227–270.
   DOI: 10.1039/d5np00066a
   —— 综述I类萜烯合酶骨架多样性的工程改造策略与应用。

与本赛题的关联：上述工作的核心问题之一是"酶的序列/结构相似性在多大程度上
能预测其催化功能"——在萜类合酶中，序列高度相似的酶常产生完全不同的骨架，
而序列疏远的酶可能催化同类反应。本方案第4.2.4节报告的
"分子结构相似性不足以定位响应相似的化合物"，在问题结构上与之同源。
这一背景是我们在设计化学表示的验证方案时坚持使用整化合物留出与随机标签对照的直接原因。

在研工作：一篇关于II类萜烯环化酶催化碱身份与机制的研究论文正在撰写中，
参赛者为共同第一作者。

单位：中国药科大学中药学院，天然药物活性组分与药效国家重点实验室。

---

